pluginManagement {
    plugins {
        id 'java'
        id 'org.springframework.boot' version '3.0.4'
        id 'io.spring.dependency-management' version '1.1.0'
    }
    repositories {
        gradlePluginPortal()
    }
}

rootProject.name = 'projectto'

include 'backend'